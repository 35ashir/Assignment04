1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

ans 1: getElementById: to find HTML element by its id.
getElementsByClassName: to find HTML element by its classname.
querySelectorAll: to select HTML element by using css selector

2. How do you create and insert a new element into the DOM?

   ans 2: step1 create element
   step2 add content
   sterp3 append child

3. What is Event Bubbling? And how does it work?

   ans 3: it's a javascript proccess. it's bubbling up through its parent element.

for example you click the btn. btn starts the event. and bubble up to parent. It's continue bubbling.

4. What is Event Delegation in JavaScript? Why is it useful?

   ans 4: Event Delegation is a technique where you attach an event listener to a parent element instead of attaching separate listeners to multiple child elements.

it's use of better performance.

5. What is the difference between preventDefault() and stopPropagation() method

ans5: preventDefault(): the broswer bult in behaviour
stopPropagation(): stops the event from bubbling up to parent elements
